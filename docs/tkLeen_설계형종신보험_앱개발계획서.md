# 설계형 종신보험 설계 앱 — 개발 계획서

- 작성: tkLeen · 2026-09
- 상태: **v0.9 (표준 프리셋·입력 정보 반영 팝업 2026-09-10)**
- 관련 문서: 『설계형 종신보험 검토 보고서』(PDF/HTML), 제안 슬라이드(PPTX)
- 참조 구현: `Python_Web_like_Excel` 워크북(실제 산출과정표 위험률·산식·원본 정답값), `Life_Prem_Calc`(PremiaFlow, TS 엔진·골든 테스트 구조)

---

## 0. 문서 이력과 결정 요약

### 0.1 v0.1 → v0.2 변경

- 주 사용자를 **설계사(상담 도구)**로 확정하고 고객은 읽기 전용 공유 링크로 분리했다.
- 1차 범위를 신규 설계·비교·제안서로 좁히고 **재설계 모드는 2차**로 옮겼다(규칙 R01~R03, DB 초안은 부록 B에 보존).
- 위험률표·예정이율·사업비·해약공제·저해지 가정을 실제 산출과정표 값으로 **채웠다**(§5.2).
- 프리셋 6종의 배수 벡터와 envelope 미정 기본값을 확정했다(§3.2, §3.3).
- 계산 엔진 명세(§3.4)가 워크북 산식과 1:1로 대응하도록 다시 썼고, 검증 정답값을 명기했다(§7).
- 기술 스택을 Next.js 15 기준으로 갱신하고 Supabase·Playwright·Python 신규 구현을 1차에서 제외했다.

### 0.2 인터뷰 결정 (2026-09-10, 객관식 12문항)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 주 사용자 | 설계사 상담 도구 + 고객 읽기 전용 공유 링크 |
| 2 | MVP 범위 | 신규 설계 + 비교 패널 + 제안서 출력. 재설계는 2차 |
| 3 | 저장·로그인 | 로그인 없음. 브라우저 저장 + URL 공유 링크. Supabase는 2차 |
| 4 | 위험률표 | 제7회 경험생명표만(남/여 0~112세, 표준사망률·납입면제 발생률 포함) |
| 5 | 사업비 산식 | 산출방법서형과 3이원 단순형 둘 다 지원, 가정 세트에서 선택 |
| 6 | 예정이율 기본 | 2.5% (설정 화면에서 변경) |
| 7 | 납입면제 | 포함, 기본 켜짐(사망+장해50% 이중탈퇴) |
| 8 | 저해지 | 포함, 초기 5년 고정 구간에만 적용 |
| 9 | 스케줄 편집 | 구간 카드 + 그래프 동기화. 그래프 드래그 편집은 2차 → v0.4에서 1차로 승격(§0.6) |
| 10 | 제안서 출력 | 브라우저 인쇄 페이지 → PDF. 서버 Playwright 없음 |
| 11 | 디바이스 | 데스크톱·태블릿 3열. 모바일은 공유 결과 보기 전용 |
| 12 | 추천 로직 | 니즈 접근법 + HLV 병행 전체 |

### 0.3 자체 결정 (상투적 항목)

| 항목 | 결정 | 이유 |
|---|---|---|
| 스택 | Next.js 15 App Router · React 19 · TypeScript strict · Tailwind v4 · Recharts · vitest (shadcn/ui·Zod는 v0.3에서 제외) | PremiaFlow와 같은 계열, 의존성 최소 |
| 폴더 | 앱을 프로젝트 루트에 생성, 기존 문서 4개는 `docs/`로 이동. 엔진 `lib/engine/`, 데이터 `lib/engine/data/*.json`, 테스트 픽스처 `tests/fixtures/` | 사용자 프로젝트 관행 |
| 계산 위치 | 전부 브라우저(클라이언트). 서버 컴포넌트는 라우팅만 | 110년 벡터 재계산은 수 ms |
| 정밀도 | 내부 float64 무반올림, 최종 보험료·준비금·환급금만 원본 관행대로 반올림(§3.4.9) | 골든 1원 일치 |
| 공유 링크 | 설계 상태 JSON → 브라우저 내장 `CompressionStream('deflate-raw')` → base64url → `/s#…` | 서버·외부 라이브러리 없이 공유, 해시라 서버 로그에 남지 않음 |
| 설정 화면 | `/settings`에서 가정 세트·envelope 편집, localStorage 저장, 인증 없음 | 1차 범위에 로그인 없음 |
| 참조 구현·Excel 검산 | 워크북에 원본 Excel 정답값이 박혀 있으므로 **TS 엔진 ↔ 워크북 원본값** 대조로 3중 대조를 대체. Python 새로 쓰지 않음 | 동일 정답을 두 번 만들 이유 없음 |
| 기준연납순보험료 | 실제 설계 스케줄 기준(워크북 관행). 기준보험금(정액 사업비·심사·표시 기준)은 초기 고정 구간 보험금 S0 | 두 워크북 정답값을 그대로 재현 |
| 흡연 구분 | 1차 제외 | 7회 표에 구분 없음. 회사 제공표로 교체 시 가정 세트에 열 추가 |
| UI 언어·단위 | 한국어. 금액 입력은 만원, 표시는 원 | 설계사 관행 |
| 브랜드 | v0.1 §4 그대로(Sky Blue #4A90C2, Deep Navy #1B2845, Ink #0A0A0A, Cream #FAFAF7 / Fraunces·Pretendard·JetBrains Mono) | — |

---

### 0.4 UI 인터뷰 결정 (2026-09-10, 객관식 4문항)

| # | 항목 | 결정 |
|---|---|---|
| 13 | 구현 계획 분할 | 두 개. 단계 2(온보딩·설계 캔버스·프리셋·카드 편집·결과)를 먼저, 단계 3(추천·비교·인쇄·공유·설정)은 단계 2 완료 후 별도 계획 |
| 14 | UI 라이브러리 | Recharts만 추가. 버튼·입력·카드는 Tailwind로 직접 작성. shadcn/ui·Zod는 설치하지 않음 |
| 15 | 온보딩 형태 | 한 페이지 4섹션(피보험자·가족·재무·기존 보장). 필수 3개(성별·연령·예산)만 채우면 설계로 이동 |
| 16 | UI 테스트 | 로직 단위 테스트(상태 리듀서·공유 직렬화·포맷)만 vitest. 화면은 `npm run build` 통과 + 수동 확인 |

### 0.5 자체 결정 (UI)

| 항목 | 결정 | 이유 |
|---|---|---|
| 상태 관리 | `lib/state.ts`에 `useReducer` + Context. 설계 상태 하나(`DesignState`)를 localStorage 키 `fwl:design:v1`에 자동 저장 | 화면 3개가 같은 상태를 공유, 외부 라이브러리 불필요 |
| 재계산 | 디바운스 없이 `useMemo(compute)`. 110년 벡터 계산은 수 ms | 300ms 지연이 오히려 체감 느림 |
| 입력 검증 | `<input type="number" min max step>` + 저장 시 clamp. Zod 미사용 | 필드 10여 개에 스키마 라이브러리는 과함 |
| 폰트 | Pretendard(jsdelivr CDN CSS), Fraunces·JetBrains Mono(`next/font/google`) | Pretendard는 Google Fonts에 없음 |
| 그래프 | 스케줄 = Recharts 계단형 라인(사망보험금 원) + 축하금 점 마커 + 초기 5년 음영. 결과 = 준비금·해약환급금·납입누계 라인 | 계획서 §3.7 |
| 자동 수정 범위 | E01(변경 시점을 5년으로 이동)·E04·E05(배수 클램프)만 버튼으로 자동 수정. E02·E03·E06·E07·E08은 제안 문구만 | 나머지는 사용자 의도(예산·축하금)를 바꾸는 일이라 자동화 부적절 |
| 근거 보기 | `<details>`로 10만원당 순·기준연납·영업보험료, 부가보험료 분해, N*·PVB 중간값 표 | §4 편의 원칙 6 |
| 샘플 설계 3종 | 35세 남·자녀연령형(막내 2세, 월 20만) / 45세 여·부채상환형(부채 15년, 월 30만) / 55세 남·상속준비형(월 50만) | 시작 화면 진입점, 회귀 검사용 |
| 반응형 | ≥1024px 3열 grid, 미만은 열을 탭으로 전환 | §4 |
| 단계 2 라우트 | `/`, `/start`, `/design`. `/compare`·`/print`·`/s`·`/settings`는 단계 3 계획 | 결정 13 |

### 0.6 그래프 편집기 결정 (2026-09-10, 객관식 3문항 + 요청 사항)

| # | 항목 | 결정 |
|---|---|---|
| 17 | 편집 방식 | 스케줄 그래프에서 연령을 클릭·드래그(또는 화살표 키)로 보험금을 바꾼다. 카드 편집은 그래프 아래 접힌 보조 기능 |
| 18 | 1칸 | 기준보험금의 10%. 연령 A에서 움직일 수 있는 칸 수 = A − (직전 변경점, 없으면 45세). 허용 범위 = 직전 변경점이 정한 수준(ref 직전 해) ± 칸 수 × 10%, 하한 E05·E06, 상한 3배. 기존 모양(프리셋·이전 편집)은 유지하고 그 위에 변화폭 Δ를 더한다: A부터 Δ, A 직전은 매년 한 칸(10%)을 넘지 않는 가장 짧은 램프로 그 전해 값에서 새 값까지(올리기·내리기 모두 급격한 변화 없음). 예: 평준형 40세 가입, 50세 3칸 ↑ → 47·48·49세 1.1·1.2·1.3배; 60세는 50세부터 ±10칸. 더블클릭하면 그 연령 값으로 이후를 평탄화. 직선 위에 놓인 변경점(마름모)은 자동으로 지운다. 어떤 프리셋에서 시작해도 모양이 평준형으로 바뀌지 않는다(2026-09-11 확정) |
| 19 | 뒤 구간 | 앞 연령을 움직이면 뒤 구간은 같은 폭만큼 함께 이동. 상한 3배(E04)·하한(E05 20%와 E06 1,000만원 중 큰 쪽)에서 정지 |
| 20 | 축하금 | 나이만 고르면 해당 연령 사망보험금의 10%로 자동 지정. 보험금이 바뀌면 따라간다. 여러 나이를 쉼표로 한 번에 입력한다(예: 55, 65) |
| 21 | 기준보험금·예산 | 가입 시 1억. 기준보험금은 1천만원 단위로만 조정(그래프 1칸 = 100만원 단위). 설계 후 월 보험료를 입력하면 배수는 유지한 채 기준보험금이 가장 가까운 1천만원 단위로 비례 조정 |
| 22 | 레이아웃 | 왼쪽 예산·계약·입력 요약, 오른쪽 큰 그래프 → 보험료·검증·환급금 곡선 → 카드 편집 → 근거. `<dialog>` 확대 보기 |
| 23 | 저해지 | 단순 규칙으로 교체: 납입기간 중 해약환급금 = 표준의 30%, 영업보험료 = 표준의 80%(20% 인하), 납입 완료 후는 표준과 동일. §3.4.7의 해지율 현가 방식은 폐기. 앱에 조건을 표시 |

### 0.7 단계 3 결정 (2026-09-10, 객관식 4문항)

| # | 항목 | 결정 |
|---|---|---|
| 24 | 추천 위치 | `/start` 예산 섹션 위 카드. 니즈·HLV 두 후보와 추천 프리셋을 "적용" 버튼으로 반영 |
| 25 | 공유 링크 | `/s#…` 읽기 전용(모바일 1열) + "내 설계로 가져오기". 페이로드는 설계 상태 전체(설정 포함)를 deflate-raw 압축, 비압축 폴백 |
| 26 | 제안서 | 고객용 2쪽 + 설계사용 1쪽(체크박스로 제외). 브라우저 인쇄 → PDF |
| 27 | 설정 | 가정 세트 선택 + 이율·사업비·저해지·니즈·envelope 숫자 폼. 설정은 설계 상태의 일부(`settings`)로 저장·공유·인쇄되며 새 설계·초기화에도 유지 |

### 0.8 저장소 결정 (2026-09-10)

| # | 항목 | 결정 |
|---|---|---|
| 28 | Supabase | 당분간 도입하지 않는다. 기기 간 동기화·로그인·고객 목록 서버 저장·감사추적은 보류 |
| 29 | 브라우저 보관함 | `fwl:designs:v1`에 설계를 이름으로 최대 50건 저장·전환(시작 화면). 설계 툴바의 "보관함에 저장" |
| 30 | 백업·이동 | JSON 내보내기·가져오기(공유 링크와 같은 페이로드 형식, 비압축)와 공유 링크로 기기 간 이동 |
| 31 | 준비금 표 | 설계 화면에 경과년별 표(연령·사망보험금·축하금·납입누계·적용/표준 준비금·해약환급금·환급률) 제공, CSV(BOM) 다운로드. 계약 체결 시 첨부용 |

### 0.9 재설계 결정 (2026-09-10, 객관식 4문항)

| # | 항목 | 결정 |
|---|---|---|
| 32 | 원계약 입력 | 현재 설계 + 경과년 / 보관함 항목 / 직접 입력(가입연령·성별·평준 보험금·납입기간·경과년·원계약 예정이율, 준비금·환급금은 입력 또는 엔진 계산) |
| 33 | 예산 모드 | 납입 지속(준비금 이월 + 같은 보험료) · 감액(준비금 이월 + 낮춘 보험료) · 납입 중단(해약환급금 이월, 감액완납형) |
| 34 | 기초율 | 새 스케줄은 현재 가정 세트로 산출. R01은 원계약 기초율로 "새 급부 현가 ≤ 이월액 + 미래 순보험료 현가"(기초율 차익 방지)로 검사하고, 기초율 차이의 전환 손실을 금액으로 표시 |
| 35 | 비교안 | 원계약 유지 · 감액완납 · 해지 · 재설계. 연장정기는 다음 |
| 36 | 산식·구현 | 재설계 계약 = 재설계 시점 연령에서 시작하는 신계약비 없는 계약. 이월액이 사는 완납분 K/π + 보험료가 사는 부분 G/P_G = 새 기준보험금. 해약공제 없음. R02는 스케줄 모양(상대 배수)이 5년간 원계약 이하인지로 검사(편집기 초기 고정 5년이 변경도 막음). R03은 보관함 저장 시각으로 1년 이내 경고. 결과는 보관함에 `redesign` 메타로 저장하고 `/redesign?id=`로 복원 |
| 37 | 수식 설명 | 모든 산출 수식을 `lib/formulas.json` 한곳에 두고, 앱의 각 숫자 옆 "?" 팝업, `/formulas` 페이지, `docs/산출수식_설명.md`(스크립트로 생성)가 같은 원본을 쓴다 |

### 0.10 수식 설명·Excel 검산 (2026-09-10)

| # | 항목 | 결정 |
|---|---|---|
| 37 | 수식 설명 | `lib/formulas.json` 하나를 원본으로 앱의 "?" 팝업(`FormulaHelp`), `/formulas` 페이지, `docs/산출수식_설명.md`(스크립트 생성)가 같은 내용을 보여준다 |
| 38 | Excel 검산 | `xlsx`(SheetJS) 의존성 추가. 설계 툴바 "Excel 검산": 입력·가정 / 위험률·계산기수 / 표준기초 계산기수 / 보험료 산출(엔진 값 + PVB·N*·순보험료 Excel 수식) / 준비금·환급금 / 설계 스케줄 6시트 |

### 0.11 표준 프리셋과 입력 정보 반영 (2026-09-10, 객관식 3문항)

| # | 항목 | 결정 |
|---|---|---|
| 39 | 시작 상태 | 설계는 항상 기준보험금 1억 + 표준 경계의 프리셋으로 시작한다. 입력 화면의 정보는 참고용이며 자동 반영되지 않는다 |
| 40 | 표준 경계 | 가입 후 경과년 기준: 자녀연령형 20년 후 30%(막내 5세 가정), 부채상환형 5년 후부터 20년째까지 선형 감액, 단체보험보완형·은퇴증액형 65세(은퇴시기 공유), 상속준비형 그대로 |
| 41 | 입력 정보 반영 | 프리셋 경계 4종(자녀 독립·부채 만기·단체보험 만기·은퇴 연령)은 해당 프리셋 카드 아래 체크박스(기본 해제)로 반영하고, 옆의 "?"가 그 조건의 근거만 보여준다(`lib/preset-info.ts`). 기준보험금(유지·니즈·HLV)과 추천 프리셋은 "기준보험금·추천 반영" 팝업에서 적용. 반영한 항목은 `infoApplied` 플래그로 저장되어 프로필을 바꾸면 따라간다. 추천 카드는 정보 표시만 |
| 42 | 설계 초기화 | 설계 툴바 "설계 초기화": 입력·설정·계약 조건은 두고 기준보험금 1억·표준 평준형·변경점/축하금/반영 플래그 초기화 |
| 43 | 자녀연령형 | 독립 시점에 갑자기 30%로 떨어지지 않고 독립 6년 전부터 매년 10%씩 내려 독립 시점에 30% |
| 44 | 입력 화면 제거 | `/start`를 없애고 입력을 설계 화면으로 옮긴다. 피보험자 카드(예산 위)는 성별·연령·배우자만 편집하는 정보성 카드. 연소득·유동자산·기존 보장과 니즈·HLV 금액·추천 프리셋은 평준형 카드의 "입력" 팝업(재무정보·기준보험금)에서 적용. 추천 카드 삭제 |
| 47 | 프리셋 램프 | 프리셋의 증액도 편집 규칙과 같이 매년 한 칸(기준보험금의 10%): 은퇴증액형 은퇴 5년 전부터 +10%/년 → 1.5배, 단체보험보완형 퇴직 5년 전부터 +10%/년 0.5→1.0, 상속준비형 5년 후 +10%/년(기준보험금 기준) → 15년 뒤 2배 또는 70세 고정. 자녀연령형·부채상환형 감액은 원래 매년 10% 이내 (2026-09-11) |
| 46 | 수식 표시 | `lib/formulas.json`의 평문 수식(`l_{x+1}`, `v^t`)을 `lib/formula-text.ts`가 아래·위첨자 조각으로 나눠 "?" 팝업·`/formulas`는 `<sub>/<sup>`(components/formula-text.tsx), MD 문서는 `<pre>` + HTML 첨자로 그린다. 라이브러리 없음(KaTeX는 분수·Σ 상하한 세로 배치가 필요해질 때) |
| 45 | 프리셋 카드 컨트롤 | 평준형: "연소득 반영" 체크(니즈 기준보험금 적용, 해제 시 1억) + "입력"(재무 팝업). 자녀연령형: 체크 + 자녀 나이 쉼표 입력(입력 시 자동 체크). 부채상환형: 체크 + "입력" 팝업(잔액·만기). 은퇴증액형·단체보험보완형: 은퇴시기 55/60/65/70 선택을 공유(단체보험 만기 = 은퇴시기, `group` 플래그·`groupCoverEndAge` 경계 제거) |

## 1. 목적과 범위

계약자가 연령별 보험금 스케줄을 설계하면 시스템이 규정과 제약(envelope)을 검증하고 평준보험료·준비금·해약환급금·사업비 흐름을 즉시 산출하는 웹 앱을 만든다. **설계사가 상담 자리에서 입력→설계→결과를 한 화면에서 보며 고객에게 제안**하는 것이 1차 목적이며, 같은 엔진이 보험사 제안 시 산출 근거가 된다.

### 1.1 1차(MVP) 포함

- 온보딩 입력(피보험자·가족·재무·기존 보장)과 니즈·HLV 기반 추천
- 프리셋 6종 + 구간 카드 편집, 예산 슬라이더(월 보험료 ↔ 초기 보험금 역산)
- envelope 검증(E01~E09)과 인라인 수정 제안
- 보험료(순·영업), 준비금, 해약환급금, 사업비 흐름, 납입면제·저해지 옵션
- 비교 패널(같은 월 예산: 평준형 종신 / 정기+종신 조합 / 설계형)
- 제안서 인쇄 페이지(PDF), 고객 공유 링크(모바일 읽기 전용)
- 설정 화면(가정 세트·envelope 파라미터 편집)

### 1.2 2차 이후

- 기존 계약 재설계 모드(준비금·미래 보험료를 예산으로, 규칙 R01~R03, 전환 손실 표시) — 부록 B
- Excel 검산 파일 내보내기, 계리 요약 PDF(입력 스냅샷·가정 버전·검증 통과 여부)
- Supabase 저장·고객 목록·로그인, 그래프 위 드래그 편집
- 회사 연계(가정 시트 업로드, 민감도 분석, 계약관리 API 규격)

### 1.3 제외

- 실제 청약·계약 체결, 본인인증, 결제, 회사 시스템 실시간 연동

---

## 2. 사용자와 시나리오

| 사용자 | 시나리오 |
|---|---|
| 설계사(주 사용자) | 고객 상황을 입력하고 추천 스케줄을 받아 프리셋 2~3개를 비교 제시한다. 예산 슬라이더로 조정한 뒤 제안서 PDF와 공유 링크를 고객에게 보낸다 |
| 고객 | 공유 링크를 휴대폰에서 열어 스케줄 그래프·보험료·환급금 곡선을 읽는다. 편집은 하지 않는다 |
| 검토자·계리(tkLeen) | 설정 화면에서 예정이율·사업비·envelope를 바꿔 민감도를 보고, 골든 테스트로 산식을 검증한다 |

---

## 3. 기능 요구사항

### 3.1 입력

| 항목 | 내용 | 비고 |
|---|---|---|
| 피보험자 | 성별, 가입연령 | 위험률 조회 키. 최종연령은 표에서 자동(남 110세·여 112세) |
| 가족 | 자녀 연령(복수), 배우자 유무 | 추천 로직·프리셋 경계 |
| 재무 | 연소득, 유동자산, 부채 잔액과 만기, 은퇴 예정 연령(기본 65) | 니즈·HLV 산출 |
| 기존 보장 | 단체보험·정기보험 보험금과 만기 | 니즈에서 차감, 단체보험보완형 경계 |
| 예산 | 월 보험료 **또는** 초기 보험금 중 하나 | 서로 역산, 슬라이더 연동 |
| 계약 조건 | 납입기간(5·10·15·20·30년, 기본 20), 납입면제(기본 ON), 저해지(기본 OFF) | 저해지는 초기 5년 고정 구간에만 |
| 가정 세트 | 기본 `default-2026`(§5.2). 설정 화면에서 교체·편집 | 산출물에 세트 id·버전 기록 |

흡연 여부, 재설계 전용 입력(기존 보험금·잔여 납입기간·현재 준비금·원계약 예정이율)은 2차.

### 3.2 스케줄 설계

**데이터 모델.** 스케줄은 구간 카드의 배열이다.

```ts
type Block = { fromAge: number; toAge: number; multiple: number; kind: 'death' | 'celebration' }
// death: 해당 연령 구간의 사망보험금 = S0 × multiple
// celebration: fromAge 시점 1회 생존급부(축하금) = S0 × multiple
```

카드를 연 단위로 전개해 `S_t`(사망보장 배수, t = 0 … n−1)와 `C_t`(생존급부 배수, 시점 t)를 만든다. 편집 단위 1년, 화면 눈금 5년. 예산 슬라이더는 **S0만** 비례 조정하고 배수 벡터는 유지한다(납입기간 연금현가가 고정이므로 보험료가 선형으로 움직인다).

**프리셋 6종 (배수 벡터 확정 초안).** x = 가입연령, S0 = 초기 고정 구간 보험금. 모든 변경은 t ≥ 5(E01)부터, 증액 램프는 연 20% 이내(E02), 감액은 자유.

| 프리셋 | 배수 정의 | 파라미터(기본) | 대상 |
|---|---|---|---|
| 평준형 | 전 구간 1.0 | — | 기준선·비교용 |
| 자녀연령형 | 막내 독립(25세) 6년 전부터 매년 −0.1, 독립 시점부터 0.3 (v0.9) | 막내 나이, 독립 연령 25 | 30대 맞벌이·영유아 자녀 |
| 부채상환형 | t=5부터 부채 만기까지 1.0 → 0.3 선형 감액, 이후 0.3 유지 | 부채 만기 연도 | 자영업자·부채 보유 |
| 은퇴증액형 | 은퇴−3년까지 1.0, 3년 램프 1.0→1.145→1.31→1.5, 이후 1.5 | 은퇴 연령 65, 목표 1.5 | 상속·정리자금 |
| 단체보험보완형 | 단체보험 만기−4년까지 0.5, 4년 램프 0.6→0.72→0.86→1.0, 이후 1.0 | 단체보험 만기 60 | 단체보험 보유 직장인 |
| 상속준비형 | t<5: 0.5, 이후 연 +10% 체증(0.5×1.1^(t−4)), 2.0 도달 또는 증액 종료 연령(E03)에서 고정 | 목표 배수 2.0 | 50대 상속 준비 |

경계가 초기 고정 구간(t<5) 안에 오면 변경 시점을 t=5로 미룬다. 증액 램프 시작이 E03을 넘으면 프리셋이 아니라 검증 위반으로 표시한다.

**블록 유형 태그.** 순수보장 / 생존급부(축하금) / 저해지 적용 구간(t<5)을 카드와 그래프에 색으로 표시한다.

### 3.3 제약 검증기(envelope)

순수 함수 `validate(schedule, params, context) → Violation[]`. 위반은 코드·메시지·해당 카드 id·수정 제안을 담는다.

| 코드 | 규칙 | 기본값 | 수정 제안 |
|---|---|---|---|
| E01 | 초기 고정 기간(t < 5) 보험금 변경 불가 | 5년 | 변경 시점을 t=5로 이동 |
| E02 | 연 증가율 상한 | 20% | 램프 연장 |
| E03 | 증액 종료 연령 | 70세 | 램프를 70세 전에 종료 |
| E04 | 최대 배수(S0 대비) | 3.0배 | 배수 축소 |
| E05 | 감액 하한(S0 대비) | 20% | 0.2로 상향 |
| E06 | 최소 보험금 절대액 | 1,000만원 | S0 상향 또는 배수 상향 |
| E07 | 심사 한도: max(S_t·S0) ≤ 언더라이팅·재정심사 한도 | 10억(입력) | S0 축소 |
| E08 | 생존급부 누계 ≤ 그 시점까지 누적 납입 영업보험료 (보장성보험 요건) | 고정 | 축하금 축소·지연 |
| E09 | 저해지 적용 구간 ⊆ 초기 고정 구간 | 구조상 자동 충족 | — |

파라미터는 가정 세트와 별도로 `envelope` 객체에 두고 설정 화면에서 편집한다. 재설계 규칙 R01~R03은 부록 B.

### 3.4 계산 엔진 명세 (v0.1)

워크북 `premium-term`(정기, 7회 표)·`whole-life-multi`(종신 다급부) 산식을 그대로 옮긴다. 두 워크북의 정답값을 **같은 코드**로 재현하는 것이 완료 조건이다(§7).

#### 3.4.1 기호

| 기호 | 뜻 |
|---|---|
| x | 가입연령 |
| ω | 표에서 q=1이 되는 나이: 남 110, 여 112 |
| n | 보험기간 = ω − x. 사망보장 연령 x … x+n−1 (ω세 사망은 원본 관행대로 제외, 기여도 10만원당 0.002원 미만) |
| m | 납입기간 |
| mm | 연 납입 횟수 = 12 (월납) |
| i, v | 예정이율, v = 1/(1+i) |
| q_t, f_t | t년차 사망률, 납입면제 사유(장해 50% 이상) 발생률. 납입면제 OFF면 f_t = 0 |
| S_t, C_t | 사망보장 배수, 생존급부 배수(기준보험금 S0 = 1 단위 기준) |

모든 산출은 **기준보험금 1단위(10만원)당**으로 하고 마지막에 S0를 곱한다.

#### 3.4.2 생존자표와 계산기수

```
lx[0]  = lx′[0] = 100000
lx[t+1]  = lx[t]  · (1 − q_t)                        # 급부 집단: 사망만
lx′[t+1] = lx′[t] · (1 − q_t − f_t + q_t·f_t/2)      # 납입 집단: 사망 + 납입면제
Dx[t]  = lx[t]·v^t          D′x[t] = lx′[t]·v^t
Cx[t]  = lx[t]·q_t·v^(t+0.5)                          # 연중앙 사망
Nx[t]  = Σ_{u≥t} Dx[u]      N′x[t] = Σ_{u≥t} D′x[u]   # 역누적
```

급부는 lx, 납입은 lx′로 센다. 납입이 면제된 계약도 보장은 유지된다.

#### 3.4.3 급부 현가와 순보험료

```
PVB  = Σ_{t<n} S_t·Cx[t] + Σ_t C_t·Dx[t]              # 가변 스케줄이므로 Mx 차분 대신 직접 합
N*   = mm·[ (N′x[0] − N′x[m]) − (mm−1)/(2·mm)·(D′x[0] − D′x[m]) ]   # 월납 보정 납입기수
P    = PVB / N*                                        # 순보험료(1회 납입, 월)
base = PVB / (N′x[0] − N′x[M]),  M = min(n, 20)         # 기준연납순보험료
```

평준 스케줄(S_t ≡ 1)이면 PVB = Mx[0] − Mx[n]으로 종신 공식과 같고, 정기 형태(만기 이후 0)면 정기 공식과 같다(§7 G3·G4).

#### 3.4.4 영업보험료

사업비 기호는 엔진 고유명으로 통일한다. **두 워크북은 α1/α2와 β1/β2의 뜻이 서로 뒤바뀌어 있으므로** 값을 옮길 때 아래 표로 매핑한다.

| 엔진 기호 | 뜻 | `premium-term`(경영인정기) | `whole-life-multi`(종신공제) |
|---|---|---|---|
| α_S | 신계약비 정액(기준보험금 비례) | α2 = 10/1000 | α1 = 10/1000 |
| α_P | 신계약비율(기준연납순보험료 비례) | α1 = 5%×min(n,20) | α2 = 5%×min(n,20) |
| β_S | 유지비 정액(기준보험금 비례, 납입 1회당 /mm) | β2 = 1.5/1000 | β1 = 0.4/1000 |
| β_G | 유지비율(영업보험료 비례) | β1 = 4.5% | β2 = 7% |
| β′ | 납입 후 유지비 정액 | 1/1000 | 0.4/1000 |
| γ | 수금비율(영업보험료 비례) | 2.5% | 4% |

**산출방법서형** (`expenseModel: 'method'`):

```
G = [ P + (α_S + α_P·base)·D′x[0]/N* + β_S/mm + β′·(Nx[m] − Nx[n])/N* ] / (1 − β_G − γ)
```

**3이원 단순형** (`expenseModel: 'simple'`, PremiaFlow 방식 A와 동일 구조):

```
G = [ P + α·D′x[0]/N* + β·(Nx[0] − Nx[n])/N* ] / (1 − γ)      # α: 가입금액 비례 1회, β: 가입금액 비례 연간
```

- 신계약비(1단위당) = α_S + α_P·round(base, 5). 원본은 기준연납을 소수 5자리로 반올림한 뒤 곱한다.
- 부가보험료 분해(신계약비·유지비·수금비·납입후유지비)와 연도별 사업비 흐름을 함께 반환한다.
- 예산 역산: S0 = 월 예산 / G(1단위). 스케줄이 바뀌면 G가 바뀌므로 다시 계산한다.

#### 3.4.5 준비금

연말 책임준비금(순보식 + 납입후유지비), 1단위당:

```
P_β   = ( PVB + β′·(Nx[m] − Nx[n]) ) / (N′x[0] − N′x[m])                 # β′ 포함 연납순보험료
V_t   = [ Σ_{u≥t, u<n} S_u·Cx[u] + Σ_{u>t} C_u·Dx[u]
        + β′·(Nx[max(t,m)] − Nx[n])
        − P_β·(N′x[t] − N′x[m])·[t ≤ m] ] / Dx[t]
```

표준기초(표준이율·표준사망률·표준발생률)로 같은 식을 돌린 값이 표준준비금이다(1차에서는 표시·검증용).

#### 3.4.6 해약환급금

```
신계약비 α  = min( 적용기초 α, 표준기초 α )                   # α = α_S + α_P·round(base,5)
해약공제_t  = α · max(min(m,7) − t, 0) / min(m,7)             # 7년 상각
W_t         = max(V_t − 해약공제_t, 0)
환급률_t    = W_t / ( min(t,m)·mm·G )
```

#### 3.4.7 저해지 (초기 5년 고정 구간) — v0.4 §0.6 #23으로 대체됨

가정 세트 `lowSurrender: { ratio: 0.5, lapse: 0.04 }`. 해지율 4%는 무해지 워크북의 가정을 따른다.

```
W^저_t = ratio·W_t   (1 ≤ t ≤ 5),   W_t (t > 5)
ΔP     = Σ_{t=1..5} lx′[t]·lapse·(1 − ratio)·W_t·v^t / N*      # 해지 시 덜 돌려주는 금액의 현가를 보험료에서 차감
P^저   = P − ΔP,  G^저는 P^저로 3.4.4 재계산
```

`ponytail:` 해지자의 납입 중단을 N*에 반영하지 않은 단순화. 승격 경로는 `nonsurrender` 워크북의 해지율 이중탈퇴 현금흐름과 표준해약공제 한도(`S = min(rounddown(net/term, 2), 1)`) 방식.

#### 3.4.8 비교 지표

초기 보험금(S0), 보험금 현가(PVB·S0), 총납입(G·mm·m), 납입 완료 시점 환급률, 보장 배수(max S_t·S0 / 연소득).

#### 3.4.9 반올림 관행 (원본 일치)

- 내부 계산은 반올림하지 않는다.
- 10만원당 순·영업보험료, 준비금, 신계약비는 원 단위 반올림(half-up, Excel ROUND)하고 그 값에 가입금액 배수를 곱한다.
- 해약환급금은 가입금액 기준으로 원 단위 반올림한다.
- 기준연납순보험료는 신계약비 산출 시에만 소수 5자리 반올림값을 쓴다.

#### 3.4.10 구현 원칙

- `lib/engine/`은 순수 TypeScript 모듈, UI·브라우저 API 의존 없음. `compute(input, assumptions) → Result` 한 진입점.
- 입력은 number input 범위 + clamp로 검증(v0.3). 결과에는 입력 스냅샷·가정 세트 id·버전·envelope 통과 여부를 함께 담는다(감사추적).
- 계산 경로에 LLM·`eval` 사용 금지(PremiaFlow 원칙).

### 3.5 추천 로직 (니즈 + HLV)

가정 세트 `needs: { discount: 0.02, livingRatio: 0.7, selfRatio: 0.3, educationPerChild: 1억, finalExpense: 3천만, independenceAge: 25, retirementAge: 65 }`. 확정연금 현가 `a(k) = (1 − (1+r)^−k)/r`.

1. **니즈 접근법.** 필요보장 = 연소득×livingRatio×a(막내 25세까지 연수) + 자녀 수×educationPerChild + 부채 잔액 + finalExpense − 기존 보장 − 유동자산.
2. **HLV.** 인적자본 = 연소득×(1 − selfRatio)×a(은퇴까지 연수).
3. 두 값을 초기 보험금 후보로 나란히 보여 주고, 가족·부채·기존 보장 정보로 프리셋을 고른다: 자녀 있음 → 자녀연령형, 부채 만기 있음 → 부채상환형, 단체보험 있음 → 단체보험보완형, 50세 이상·자녀 독립 → 상속준비형, 그 외 → 평준형. 구간 경계(막내 25세, 부채 만기, 단체보험 만기, 은퇴)를 자동 설정한다.
4. 결과를 envelope에 투영(E04·E05·E06 클램프, E01 경계 이동)한 뒤 예산 입력 시 S0를 비례 조정한다.

### 3.6 비교 패널

같은 **월 예산** B에서 세 안의 S0를 각각 역산한다.

| 안 | 정의 |
|---|---|
| 평준형 종신 | S_t ≡ 1, m년납 |
| 정기 + 종신 조합 | 종신 S_W = 설계형 최종 구간 배수, 정기 S_T = S0 − S_W(만기 = 설계형의 첫 감액 시점, 납입 = min(m, 만기)). 두 계약을 같은 가정 세트로 각각 산출해 합산 |
| 설계형 | 현재 스케줄 |

지표 표(§3.4.8) + 그래프 겹쳐 보기. 조합안의 정기 사업비는 종신과 같은 세트를 적용한다(§8 리스크).

### 3.7 산출물

| 산출물 | 내용 |
|---|---|
| 화면 | 스케줄 그래프(배수·금액 토글), 보험료 요약, 준비금·환급금 곡선, 사업비 흐름, 검증 배지, 산출 근거 펼침 |
| 제안서(인쇄) | `/print`: A4 세로. 1~2쪽 고객용(스케줄·보험료·환급금·비교 표·가정 요약), 3쪽 설계사용(입력 스냅샷·가정 세트 id·envelope 통과 여부·부가보험료 분해). `@media print`로 브라우저 "PDF로 저장" |
| 공유 링크 | `/s#<base64url>`: 모바일 세로 1열 읽기 전용. 링크에 설계 상태·가정 세트 id·(사용자 정의 가정이면 값 전체)·앱 버전을 담는다 |
| Excel 검산 파일 | 2차 |

---

## 4. 화면 구성

| 화면 | 라우트 | 주요 요소 |
|---|---|---|
| 시작 | `/` | 마지막 설계 자동 복원 또는 온보딩 시작. 샘플 설계 3개 |
| 온보딩 | `/start` | 한 페이지 4섹션(피보험자 → 가족 → 재무 → 기존 보장). 기본값만으로 설계 진행 가능 |
| 설계 캔버스 | `/design` | **3열**: 입력 요약·예산 슬라이더 | 프리셋 선택·구간 카드·스케줄 그래프 | 보험료·환급금 곡선·검증 배지 |
| 비교 | `/compare` | 3안 카드 + 지표 표 + 겹친 그래프 |
| 인쇄 | `/print` | 제안서 레이아웃 |
| 공유 보기 | `/s` | 모바일 읽기 전용 |
| 설정 | `/settings` | 가정 세트 편집·복제, envelope 파라미터, 골든 케이스 재계산 결과 |

태블릿 가로(≥ 1024px)에서 3열, 그 미만은 열을 탭으로 전환. 모바일 세로에서는 `/design` 대신 `/s` 형태의 결과 보기를 연다.

**사용자 편의 원칙**

1. 입력 변경 즉시 재계산(`useMemo`). 저장 버튼 없음(localStorage 자동 저장).
2. 기본값만으로 결과에 도달한다. 필수 입력은 성별·연령·예산 3개.
3. envelope 위반은 해당 카드에 인라인으로 표시하고 "자동 수정" 버튼을 제공한다.
4. 금액 입력은 만원 단위, 표시는 원 단위 천 단위 구분. 배수 ↔ 금액 토글.
5. 프리셋을 먼저 노출하고 커스텀 카드 편집은 접힌 상태로 둔다.
6. 결과 화면의 모든 숫자는 "근거 보기"로 산식·중간값을 펼칠 수 있다.

---

## 5. 기술 스택과 데이터

### 5.1 스택

| 계층 | 선택 |
|---|---|
| 프런트 | Next.js 15 App Router, React 19, TypeScript strict, Tailwind v4, Recharts |
| 엔진 | `lib/engine` 순수 TS, vitest 골든 테스트 (완료 2026-09-10, G1~G5·envelope 39 테스트) |
| 데이터 | 위험률·가정 세트 JSON을 저장소에 포함. 사용자 설정·설계는 localStorage |
| 공유 | URL 해시 + `CompressionStream` (미지원 브라우저는 비압축 base64url) |
| 문서 | 인쇄용 라우트 + `@media print` |
| 배포 | Vercel (서버 함수 없음) |

Supabase·Playwright·Python·xlsx는 1차에 설치하지 않는다.

### 5.2 가정 세트

```ts
type AssumptionSet = {
  id: string; version: string; label: string
  mortality: 'kli7'                       // lib/engine/data/rates-kli7.json
  interest: number; standardInterest: number
  expenseModel: 'method' | 'simple'
  expenses: { alphaS: number; alphaP: number; betaS: number; betaG: number; betaPrime: number; gamma: number }
         | { alpha: number; beta: number; gamma: number }
  waiver: boolean
  lowSurrender: { ratio: number; lapse: number }
  needs: { discount: number; livingRatio: number; selfRatio: number; educationPerChild: number; finalExpense: number; independenceAge: number; retirementAge: number }
}
```

기본 세트 `default-2026`

| 항목 | 값 | 출처 |
|---|---|---|
| 위험률 | 제7회 경험생명표 남/여 0~112세, 표준사망률, 납입면제 발생률·표준발생률 | `premium-term.pygrid.json` 위험률 시트(경영인정기보험 무배당 1504 산출과정표) |
| 예정이율 | 2.5% | 인터뷰 결정 |
| 표준이율 | 3.25% | 원본 조회 시트 |
| 사업비(산출방법서형) | α_S 10/1000, α_P 5%×min(n,20), β_S 1.5/1000, β_G 4.5%, β′ 1/1000, γ 2.5% | 경영인정기 1504 |
| 납입면제 | ON | 인터뷰 결정 |
| 저해지 | ratio 0.5, lapse 4% | 무해지 워크북 가정 |
| 니즈 | discount 2%, livingRatio 70%, selfRatio 30%, 교육비 1억/인, 정리자금 3천만, 독립 25세, 은퇴 65세 | 자체 기본값 |

동봉 프리셋

- `verify-term-1504`: 위 세트에 예정이율 3.4% — 골든 G1 재현용
- `expense-wholelife-mutual`: 종신공제 사업비(α_S 10/1000, α_P 5%×min(n,20), β_S 0.4/1000, β_G 7%, β′ 0.4/1000, γ 4%)
- `expense-simple`: 3이원 단순형(α 0.7%, β 0.15%, γ 2%)

### 5.3 위험률 데이터 추출

`scripts/extract-rates.mjs`(1회 실행, 결과 파일을 커밋)

- 입력: `../Python_Web_like_Excel/data/sample-workbooks/premium-term.pygrid.json`. 셀 키는 `"row:col"`, 시트 `위험률` 11열(나이, 경험사망률_남/여, 표준사망률_남/여, 우량체사망률_남/여, 발생률_남/여, 표준발생률_남/여), 나이 0~112. 남 q=1 at 110, 여 q=1 at 112.
- 출력: `lib/engine/data/rates-kli7.json`

```json
{ "meta": { "name": "제7회 경험생명표", "source": "경영인정기보험 무배당 1504 산출과정표 (premium-term.pygrid.json)", "ages": [0, 112], "terminal": { "M": 110, "F": 112 } },
  "M": { "q": [], "f": [], "qStd": [], "fStd": [] },
  "F": { "q": [], "f": [], "qStd": [], "fStd": [] } }
```

- 테스트 전용: `whole-life-multi.pygrid.json`의 써미트 2014-59호 사망률·장해50(0~113세)을 `tests/fixtures/rates-summit.json`으로 추출한다. 가정 세트로는 등록하지 않는다.

### 5.4 폴더 구조

```
flexible_whole_life/
├─ app/            (start|design|compare|print|s|settings)/page.tsx, layout.tsx
├─ components/     canvas/ (BlockCard, ScheduleChart, BudgetSlider) result/ compare/ print/
├─ lib/engine/     rates.ts commutation.ts premium.ts reserve.ts surrender.ts lowSurrender.ts
│                  schedule.ts presets.ts envelope.ts needs.ts compare.ts index.ts
│                  data/ rates-kli7.json assumptions.json
├─ lib/            state.ts (useReducer + Context + localStorage) samples.ts format.ts share.ts(단계 3)
├─ tests/          engine/*.test.ts fixtures/
├─ scripts/        extract-rates.mjs
└─ docs/           계획서·보고서·슬라이드
```

---

## 6. 개발 단계

| 단계 | 기간(안) | 산출 | 완료 조건 |
|---|---|---|---|
| 0. 명세·데이터 | 완료(2026-09-10) | 이 문서, `rates-kli7.json`, 골든 픽스처 | 추출 JSON 행 수 113, 남 110·여 112에서 q=1 ✔ |
| 1. 엔진 | 완료(2026-09-10) | `lib/engine` 전 모듈 + vitest | G1~G5 통과, envelope 케이스 통과 ✔ |
| 2. 설계 화면 | 완료(2026-09-10) | 온보딩, 2열 캔버스, 프리셋, 그래프 단계 편집·카드 편집, 결과, 준비금 표 CSV | 기본값만으로 결과 도달, 위반 인라인 표시 ✔ |
| 3. 제안 기능 | 완료(2026-09-10) | 추천, 비교, 인쇄, 공유 링크, 설정, 브라우저 보관함·JSON | 상담 시나리오·모바일 공유 열람 브라우저 검증 ✔ |
| 4. 2차 | 완료(2026-09-10) | 재설계(§0.9), Excel 검산(§0.10), 수식 설명(§0.10). 그래프 드래그 편집은 2.5단계에서 완료 | Supabase만 보류(§0.8) |

---

## 7. 검증 계획

### 7.1 골든 케이스 (TS 엔진 ↔ 워크북 원본 Excel 값)

허용 오차: 10만원당 1원(원본은 ROUND 후 정수), 중간값은 상대 1e-9.

**G1 정기 특수해 — 7회 표, `premium-term` 원본 대조표**

- 입력: 31세 남, 만기 90(S_t = 1 for t < 59, 이후 0, C_t = 0), 20년납 월납, 가입금액 1억, i 3.4%, 표준이율 3.25%, 산출방법서형 사업비(경영인정기 1504 값), 납입면제 ON
- 중간값: M* 16,212.828499, N* 17,378,602.403207
- 10만원당: 기준연납 1,102 · 순 93 · 총신계약비 2,102 · β′포함 연납순 1,167 · 영업 133 · 총납입 31,920
- 가입금액당 월 영업보험료 133,000원
- 해약환급금(1억): 1년 0 · 2년 858,571 · 3년 2,397,857 · 5년 5,593,429 · 10년 13,417,000 · 20년 31,346,000 (환급률 0 / 26.9 / 50.1 / 70.1 / 84.1 / 98.2%)
- 적용 준비금(10만원당): 1년 1,161 · 3년 3,599 · 5년 6,194 · 10년 13,417 · 20년 31,346 · 40년 46,491
- 표준 준비금(10만원당): 1년 1,237 · 3년 3,833 · 5년 6,592 · 10년 14,237 · 20년 33,086 · 40년 49,450
- 표준기초 보험료: 순 100 · 영업 141

**G2 종신 다급부 — 써미트 픽스처, `whole-life-multi` 원본 총괄 대조표**

- 입력: 59세 남, i 3.5%, 5년납 월납, S_t = 2 (59~64세) · 1 (65~109세), 축하금 C = 1.0 at 65세, 납입면제 ON(장해50), 종신공제 사업비(α_S 10/1000, α_P 1.0, β_S 0.4/1000, β_G 7%, β′ 0.4/1000, γ 4%), M = 20
- 중간값: A 종신사망 45,506.19015146 · B 정기사망 앞 3,983.43528567 · C 건강축하금 77,740.6234102 · SUMX 127,230.24884734 · N′x 1,549,189.8965 · N* 5,413,118.70895 · 기준연납 분모 1,319,222.0502 · MaxANP 0.09644339 · P 0.02350406 · PP 0.02875334
- 10만원당: 순 2,350 · 영업 2,875 · 신계약비 10,644 · 신계약비 한도 11,644 · 위험보험료 457 · 저축보험료 2,418
- 일시납: 순 1.27230249 · 영업 1.38649054 · 10만원당 138,649 (일시납은 참고, 1차 UI 미노출)

**G3 평준 스케줄 = 종신 공식.** S_t ≡ 1, 납입면제 OFF, 사업비 0 → P·N* / PVB = 1이고 PVB = Mx[0] − Mx[n]. PremiaFlow `pvBenefit`·`annualNetPremium`과 연납 기준 동치.

**G4 정기 형태 = 정기 공식.** G1 입력에서 사업비 0 → 순보험료 = (Mx[0] − Mx[59]) / N*.

**G5 예산 역산 왕복.** 임의 스케줄에서 S0 → G → 예산 → S0 복원, 상대오차 1e-12.

### 7.2 규칙·기능 테스트

- E01~E09 각각 위반 1건·통과 1건. 프리셋 6종(기본 파라미터, 40세 남)이 기본 envelope를 통과.
- 저해지: t ≤ 5 환급금 = ratio × 표준, t > 5 동일, G^저 < G.
- 공유 링크 직렬화 왕복(압축·비압축 둘 다), 앱 버전 불일치 시 경고.
- 추천 로직: 자녀 2명(3세·6세)·연소득 6천만·부채 2억 입력의 니즈·HLV 값을 손계산과 대조.

### 7.3 실행

```bash
npm run build        # 빌드·타입 오류 0
npx vitest run       # 단위 + 골든
```

가정 세트 버전을 올릴 때는 저장된 샘플 설계 3개를 재계산해 차이를 보고한다(회귀).

---

## 8. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 7회 표 시점(현행 10회 대비 사망률 높음 → 보험료 보수적) | 위험률은 가정 세트 교체로 바뀌도록 설계. 회사 제공표는 같은 JSON 형식으로 추가 |
| 흡연·우량체 구분 없음 | 1차 제외. 표에 열 추가로 확장 |
| 저해지 단순화(§3.4.7) | `ponytail:` 표시, 승격 경로 명시. 제안서에는 "저해지 보험료는 참고" 문구 |
| 정기+종신 조합 비교의 정기 사업비 가정 | 같은 세트 적용을 화면에 명시 |
| 설명 복잡도·감독 스탠스(체증형 소비자경보) | 프리셋 우선, 커스텀 접힘, 비교 화면을 설명의무 자료로 |
| 브라우저 저장 유실 | 공유 링크가 곧 백업. 설계 JSON 내보내기·가져오기 버튼 제공 |
| 규제 변경 | envelope·보장성 요건 파라미터화, 가정 세트 버전 관리 |

---

## 9. 다음 행동

1. 실제 상담에서 써 보고 설계 규칙(1칸 10%·축하금 10%·저해지 30%/20%)과 프리셋 초기값을 조정
2. 회사 위험률·사업비 시트가 확보되면 가정 세트 JSON에 추가(설정 화면에서 선택)
3. 여러 기기·여러 설계사가 쓰게 되면 Supabase 도입 검토(§0.8, 부록 B.3)
4. 연장정기 비교안, 회사 연계(민감도 분석·API 규격)는 필요 시 별도 계획

---

## 부록 A. 참조 자산

| 자산 | 경로 | 쓰임 |
|---|---|---|
| 7회 표 + 정기 산식·정답 | `C:\00 App Project\Python_Web_like_Excel\data\sample-workbooks\premium-term.pygrid.json` — 블록 "계산기수표", "보험료 산출 · 원본 대조표", "경과년별 준비금 · 환급금 표" | 위험률 추출, §3.4.2~3.4.6, G1 |
| 종신공제 산식·정답 | `...\whole-life-multi.pygrid.json`; 생성 원본 `Python_Web_like_Excel\.claude\skills\sample-workbook-gen\scripts\build_samples.py:3000-3079` | 다급부·이중탈퇴·N*, G2 |
| 무해지 해지율·표준해약공제 한도 | `...\nonsurrender.pygrid.json` 가정 시트(해지율 4%, 공제기간 7) | §3.4.7 |
| 3이원 단순형·골든 구조 | `C:\00 App Project\Life_Prem_Calc\lib\engine\premium.ts`(`grossPremiumA`), `pv.ts`, `decrement.ts`, `docs\domain\golden-cases.md` | §3.4.4 simple, 테스트 구성 |
| 가정 시트 UX 패턴 | `Python_Web_like_Excel\components\shell\FileMenu.tsx:65-134`, 설계서 `docs\pygrid-studio-design.md` 부록 M | 설정 화면 참고 |
| 상품 개념·규제 | `docs\tkLeen_설계형종신보험_제안보고서.html` §08~§11 | envelope, 기준보험금 정의, 보장성 요건 |

## 부록 B. 2차 항목 보존 (v0.1에서 이동)

### B.1 재설계 입력 — v0.7에서 구현(§0.9)

기존 보험금, 잔여 납입기간, 현재 준비금 또는 해약환급금, 원계약 예정이율. 이 상품 계약이면 자동 조회.

### B.2 재설계 규칙 — v0.7에서 구현(§0.9)

| 코드 | 규칙 | 기본값 |
|---|---|---|
| R01 | 동일 기초율로 PV(신 스케줄) ≤ PV(구 스케줄) | 고정 |
| R02 | 재설계 후 5년 이내 연도는 기존 이하로만 변경, 증액은 6년째 이후 | 고정 |
| R03 | 재설계 횟수 제한 | 연 1회 |

재설계 예산 = V_t + PV(미래 보험료) − PV(미래 사업비). 결과 = 새 스케줄의 평준보험료 또는 완납 시 실현 가능 보험금. 비교: 원계약 유지 / 감액 / 감액완납 / 연장정기 / 해지 / 재설계. 기존 종신에서 전환 시 기초율 차이 손실을 금액으로 표시. 납입 지속 재설계는 준비금 이월, 납입 중단은 해약환급금 이월(감액완납 기준), 신계약비 미부과.

### B.3 DB 테이블 초안 (Supabase 도입 시)

- `assumption_sets`: id, version, mortality_table, interest, expense_json, lapse, created_at
- `designs`: id, user_id, input_json, schedule_json, result_json, assumption_id, envelope_version, created_at
- `redesigns`: id, design_id, source_contract_json, budget_mode, result_json, rule_checks_json
- `validation_runs`: id, assumption_id, ts_result, workbook_result, max_diff

### B.4 화면 — v0.7에서 구현(§0.9)

재설계: 기존 계약 입력 → 예산 3종(납입 지속·감액·중단) → 재설계 캔버스 → 비교. 관리자: 가정 시트 업로드, 민감도 분석, 검산 대조 로그.
